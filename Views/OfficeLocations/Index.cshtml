@model IEnumerable<team10.Models.OfficeLocation>

@{
}

<h2>Office Locations</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.locationName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.locationName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OfficeLocationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OfficeLocationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OfficeLocationID })
        </td>
    </tr>
}

</table>
