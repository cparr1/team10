@model IEnumerable<team10.Models.UserTitle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.titleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.titleName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserTitleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserTitleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserTitleID })
        </td>
    </tr>
}

</table>
